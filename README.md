## use to get next auth secret

### npx next auth

Monetization Options Later

You’ll be able to add:

⭐ Paid promotion ("featured festival")
⭐ Monthly subscription for creators
⭐ Pay-per-listed event
⭐ Venue verification fee
⭐ Ads (cultural sponsors)

Because you control both the API & data.

```bash
# On Linux/Mac
openssl rand -base64 32

# Or using Node
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```
